#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcpy - check the code
 * @dest: A string input
 * @src: A string input
 * Return: A array of string
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (src);
}
