#include "main.h"
#include <string.h>

/**
 * _strlen - check the code
 * @s: A pointer an interger input
 * Return: Length of integer
 */
int _strlen(char *s)
{
	int n;

	n = strlen(s);
	return (n);
}
