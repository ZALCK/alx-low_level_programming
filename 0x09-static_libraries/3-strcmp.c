#include "main.h"
#include <string.h>

/**
 * _strcmp - check the code
 * @s1: A pointer to a string input
 * @s2: A pointer to a string input
 *
 * Return: A positive or negative integer
 */
int _strcmp(char *s1, char *s2)
{
	int n;

	n = strcmp(s1, s2);
	return (n);
}
